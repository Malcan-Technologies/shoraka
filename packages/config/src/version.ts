/**
 * Application version
 * Update this constant when releasing new versions
 */
export const APP_VERSION = "v0.0.3-alpha";

/**
 * Get formatted version string
 */
export function getVersionString(): string {
  return APP_VERSION;
}
