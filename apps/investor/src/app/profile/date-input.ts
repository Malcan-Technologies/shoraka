/**
 * Converts API calendar-date values into the string format required by
 * `<input type="date" />` (`YYYY-MM-DD` in Asia/Kuala_Lumpur).
 */
export { toCalendarDateInput as toDateInputValue } from "@cashsouk/types";
