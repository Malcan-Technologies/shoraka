NODE_ENV=development
PORT=4000
DATABASE_URL=postgresql://postgres:password@localhost:5432/shoraka_dev

# AWS (for local S3 testing - optional)
AWS_REGION=ap-southeast-5
S3_BUCKET=
S3_PREFIX=

# Cognito (leave empty for dev, use mock auth)
COGNITO_USER_POOL_ID=
COGNITO_APP_CLIENT_ID=
JWT_ISSUER=

# CORS
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:3002,http://localhost:3003

