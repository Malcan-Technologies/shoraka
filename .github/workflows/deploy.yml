name: Deploy All Services to ECR

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  AWS_REGION: ap-southeast-5

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for changes
        id: changes
        run: |
          # Handle first commit case - compare against empty tree
          if git rev-parse HEAD^ >/dev/null 2>&1; then
            COMPARE="HEAD^"
          else
            COMPARE="4b825dc642cb6eb9a060e54bf8d69288fbee4904" # Empty tree SHA
          fi

          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/api/|^packages/(types|config)/'; then
            echo "api=true" >> $GITHUB_OUTPUT
          else
            echo "api=false" >> $GITHUB_OUTPUT
          fi

          # Check if migrations are needed (schema or migration files changed)
          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/api/prisma/(schema\.prisma|migrations/)'; then
            echo "migrations=true" >> $GITHUB_OUTPUT
          else
            echo "migrations=false" >> $GITHUB_OUTPUT
          fi

          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/landing/|^packages/'; then
            echo "landing=true" >> $GITHUB_OUTPUT
          else
            echo "landing=false" >> $GITHUB_OUTPUT
          fi

          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/investor/|^packages/'; then
            echo "investor=true" >> $GITHUB_OUTPUT
          else
            echo "investor=false" >> $GITHUB_OUTPUT
          fi

          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/issuer/|^packages/'; then
            echo "issuer=true" >> $GITHUB_OUTPUT
          else
            echo "issuer=false" >> $GITHUB_OUTPUT
          fi

          if git diff --name-only $COMPARE HEAD | grep -qE '^apps/admin/|^packages/'; then
            echo "admin=true" >> $GITHUB_OUTPUT
          else
            echo "admin=false" >> $GITHUB_OUTPUT
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # API: Build → Migrate → Push → Deploy
      - name: Build API image
        if: steps.changes.outputs.api == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: api-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building API image..."
          docker build -f docker/api.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

      - name: Build and push migration image
        if: steps.changes.outputs.migrations == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: cashsouk-migrate
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building migration image..."
          docker build -f docker/migrate.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing migration image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

      - name: Run Database Migrations
        if: steps.changes.outputs.migrations == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group \
            --log-group-name /ecs/cashsouk-migrate \
            --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📦 Registering migration task definition from template..."

          # Always use the template file to ensure latest configuration (secrets, roles, etc.)
          TASK_DEF=$(cat infra/ecs-task-definition-migrate.json | jq --arg IMAGE "$ECR_REGISTRY/cashsouk-migrate:$IMAGE_TAG" \
            '.containerDefinitions[0].image = $IMAGE')

          aws ecs register-task-definition \
            --cli-input-json "$TASK_DEF" \
            --region ${{ env.AWS_REGION }}

          echo "📦 Running migrations via ECS task..."

          # Run migration as one-off ECS task
          TASK_ARN=$(aws ecs run-task \
            --cluster default \
            --launch-type FARGATE \
            --task-definition cashsouk-migrate \
            --network-configuration "awsvpcConfiguration={subnets=[subnet-0b0afc5315186d366,subnet-01ca25e9377340988,subnet-042e311cc1ee5ebdd],securityGroups=[sg-0f699f7ae290e3979],assignPublicIp=ENABLED}" \
            --region ${{ env.AWS_REGION }} \
            --query 'tasks[0].taskArn' \
            --output text)

          if [ -z "$TASK_ARN" ] || [ "$TASK_ARN" = "None" ]; then
            echo "❌ Failed to start migration task"
            exit 1
          fi

          echo "⏳ Waiting for migrations to complete (Task: $TASK_ARN)..."

          # Wait for task to complete
          aws ecs wait tasks-stopped \
            --cluster default \
            --tasks $TASK_ARN \
            --region ${{ env.AWS_REGION }}

          # Check exit code
          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster default \
            --tasks $TASK_ARN \
            --region ${{ env.AWS_REGION }} \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)

          if [ "$EXIT_CODE" != "0" ] && [ "$EXIT_CODE" != "None" ]; then
            echo "❌ Migrations failed with exit code $EXIT_CODE"
            
            # Get task details for debugging
            echo "📋 Task details:"
            aws ecs describe-tasks \
              --cluster default \
              --tasks $TASK_ARN \
              --region ${{ env.AWS_REGION }} \
              --query 'tasks[0].containers[0]' || true
            
            # Try to get logs using the task ID
            TASK_ID=$(echo $TASK_ARN | awk -F'/' '{print $NF}')
            echo "📋 Migration logs (Task ID: $TASK_ID):"
            
            # List log streams first to see what's available
            echo "Available log streams:"
            aws logs describe-log-streams \
              --log-group-name /ecs/cashsouk-migrate \
              --region ${{ env.AWS_REGION }} \
              --max-items 5 \
              --order-by LastEventTime \
              --descending || echo "Could not list log streams"
            
            # Try to tail logs
            aws logs tail /ecs/cashsouk-migrate \
              --since 15m \
              --format short \
              --region ${{ env.AWS_REGION }} || echo "Could not retrieve logs"
            
            exit 1
          elif [ "$EXIT_CODE" = "None" ]; then
            echo "⚠️ Task stopped but exit code is None - checking task status..."
            aws ecs describe-tasks \
              --cluster default \
              --tasks $TASK_ARN \
              --region ${{ env.AWS_REGION }} \
              --query 'tasks[0]' || true
            exit 1
          fi

          echo "✅ Migrations completed successfully"

      - name: Push and deploy API
        if: steps.changes.outputs.api == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: api-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "📦 Pushing API image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group --log-group-name /ecs/cashsouk-api --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📝 Registering API task definition..."

          # Use the base template with all environment variables
          TEMPLATE=$(cat infra/ecs-task-definition-api.json)
          
          # Update only the image tag and clear secrets
          NEW_TASK_DEF=$(echo $TEMPLATE | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" '
            .containerDefinitions[0].image = $IMAGE |
            .containerDefinitions[0].secrets = []
          ')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying API to ECS..."
          aws ecs update-service \
            --cluster default \
            --service api-cashsouk-09ff \
            --task-definition default-api-cashsouk-09ff \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ API deployment triggered - new tasks will pull latest image"

      # Landing: Build → Push → Deploy (immediately)
      - name: Build push and deploy Landing
        if: steps.changes.outputs.landing == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: landing-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Landing image..."
          docker build -f docker/landing.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Landing image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group --log-group-name /ecs/cashsouk-landing --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📝 Updating task definition..."
          TEMPLATE=$(cat infra/ecs-task-definition-landing.json)
          NEW_TASK_DEF=$(echo $TEMPLATE | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" '
            .containerDefinitions[0].image = $IMAGE |
            .containerDefinitions[0].secrets = []
          ')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying Landing to ECS..."
          aws ecs update-service \
            --cluster default \
            --service landing-cashsouk-1156 \
            --task-definition default-landing-cashsouk-1156 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Landing deployed successfully"

      # Investor: Build → Push → Deploy (immediately)
      - name: Build push and deploy Investor
        if: steps.changes.outputs.investor == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: investor-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Investor image..."
          docker build -f docker/portal-investor.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Investor image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group --log-group-name /ecs/cashsouk-investor --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📝 Updating task definition..."
          TEMPLATE=$(cat infra/ecs-task-definition-investor.json)
          NEW_TASK_DEF=$(echo $TEMPLATE | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" '
            .containerDefinitions[0].image = $IMAGE |
            .containerDefinitions[0].secrets = []
          ')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying Investor to ECS..."
          aws ecs update-service \
            --cluster default \
            --service investor-cashsouk-50e5 \
            --task-definition default-investor-cashsouk-50e5 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Investor deployed successfully"

      # Issuer: Build → Push → Deploy (immediately)
      - name: Build push and deploy Issuer
        if: steps.changes.outputs.issuer == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: issuer-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Issuer image..."
          docker build -f docker/portal-issuer.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Issuer image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group --log-group-name /ecs/cashsouk-issuer --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📝 Updating task definition..."
          TEMPLATE=$(cat infra/ecs-task-definition-issuer.json)
          NEW_TASK_DEF=$(echo $TEMPLATE | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" '
            .containerDefinitions[0].image = $IMAGE |
            .containerDefinitions[0].secrets = []
          ')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying Issuer to ECS..."
          aws ecs update-service \
            --cluster default \
            --service issuer-cashsouk-ff47 \
            --task-definition default-issuer-cashsouk-ff47 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Issuer deployed successfully"

      # Admin: Build → Push → Deploy (immediately)
      - name: Build push and deploy Admin
        if: steps.changes.outputs.admin == 'true'
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: cashsouk-admin
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Admin image..."
          docker build -f docker/portal-admin.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Admin image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG

          echo "📝 Ensuring CloudWatch log group exists..."
          aws logs create-log-group --log-group-name /ecs/cashsouk-admin --region ${{ env.AWS_REGION }} 2>/dev/null || echo "Log group already exists"

          echo "📝 Updating task definition..."
          TEMPLATE=$(cat infra/ecs/task-def-admin.json)
          NEW_TASK_DEF=$(echo $TEMPLATE | jq --arg IMAGE "$ECR_REGISTRY/cashsouk-admin:$IMAGE_TAG" '
            .containerDefinitions[0].image = $IMAGE
          ')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying Admin to ECS..."
          aws ecs update-service \
            --cluster default \
            --service cashsouk-admin \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Admin deployed successfully"

      - name: Deployment summary
        run: |
          echo "✅ Deployment completed for services with changes"
          echo "Image tag: ${{ github.sha }}"
          echo ""
          echo "Services deployed:"
          if [ "${{ steps.changes.outputs.api }}" == "true" ]; then echo "  - API"; fi
          if [ "${{ steps.changes.outputs.landing }}" == "true" ]; then echo "  - Landing"; fi
          if [ "${{ steps.changes.outputs.investor }}" == "true" ]; then echo "  - Investor Portal"; fi
          if [ "${{ steps.changes.outputs.issuer }}" == "true" ]; then echo "  - Issuer Portal"; fi
          if [ "${{ steps.changes.outputs.admin }}" == "true" ]; then echo "  - Admin Portal"; fi
