name: Deploy Selected Services

on:
  workflow_dispatch:
    inputs:
      deploy_api:
        description: "Deploy API"
        required: true
        type: boolean
        default: true
      deploy_landing:
        description: "Deploy Landing"
        required: true
        type: boolean
        default: true
      deploy_investor:
        description: "Deploy Investor Portal"
        required: true
        type: boolean
        default: true
      deploy_issuer:
        description: "Deploy Issuer Portal"
        required: true
        type: boolean
        default: true
      deploy_admin:
        description: "Deploy Admin Portal"
        required: true
        type: boolean
        default: true

env:
  AWS_REGION: ap-southeast-5

permissions:
  id-token: write # Required for OIDC
  contents: read

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_DEPLOY_ROLE_ARN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      # API: Build → Migrate → Push → Deploy (immediately)
      - name: Build API image
        if: ${{ inputs.deploy_api }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: api-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building API image..."
          docker build -f docker/api.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

      - name: Check for migration changes
        id: migration_check
        if: ${{ inputs.deploy_api }}
        run: |
          # For manual deployment, always run migrations to be safe
          echo "migrations_changed=true" >> $GITHUB_OUTPUT

      - name: Build and push migration image
        if: ${{ inputs.deploy_api && steps.migration_check.outputs.migrations_changed == 'true' }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: cashsouk-migrate
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building migration image..."
          docker build -f docker/migrate.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing migration image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

      - name: Run Database Migrations
        if: ${{ inputs.deploy_api && steps.migration_check.outputs.migrations_changed == 'true' }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "📦 Registering migration task definition..."

          # Check if task definition exists, if not create it from template
          if ! aws ecs describe-task-definition \
            --task-definition cashsouk-migrate \
            --region ${{ env.AWS_REGION }} &>/dev/null; then
            echo "Creating new task definition from template..."
            TASK_DEF=$(cat infra/ecs-task-definition-migrate.json | jq --arg IMAGE "$ECR_REGISTRY/cashsouk-migrate:$IMAGE_TAG" \
              '.containerDefinitions[0].image = $IMAGE')
            aws ecs register-task-definition \
              --cli-input-json "$TASK_DEF" \
              --region ${{ env.AWS_REGION }}
          else
            echo "Updating existing task definition..."
            # Get the current task definition
            TASK_DEF=$(aws ecs describe-task-definition \
              --task-definition cashsouk-migrate \
              --region ${{ env.AWS_REGION }})
            
            # Update the image tag in the task definition
            NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/cashsouk-migrate:$IMAGE_TAG" \
              '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
            
            # Register new task definition revision
            aws ecs register-task-definition \
              --cli-input-json "$NEW_TASK_DEF" \
              --region ${{ env.AWS_REGION }} > /dev/null
          fi

          echo "📦 Running migrations via ECS task..."

          # Run migration as one-off ECS task
          TASK_ARN=$(aws ecs run-task \
            --cluster default \
            --launch-type FARGATE \
            --task-definition cashsouk-migrate \
            --network-configuration "awsvpcConfiguration={subnets=[subnet-0b0afc5315186d366,subnet-01ca25e9377340988,subnet-042e311cc1ee5ebdd],securityGroups=[sg-0f699f7ae290e3979],assignPublicIp=DISABLED}" \
            --region ${{ env.AWS_REGION }} \
            --query 'tasks[0].taskArn' \
            --output text)

          if [ -z "$TASK_ARN" ] || [ "$TASK_ARN" = "None" ]; then
            echo "❌ Failed to start migration task"
            exit 1
          fi

          echo "⏳ Waiting for migrations to complete (Task: $TASK_ARN)..."

          # Wait for task to complete
          aws ecs wait tasks-stopped \
            --cluster default \
            --tasks $TASK_ARN \
            --region ${{ env.AWS_REGION }}

          # Check exit code
          EXIT_CODE=$(aws ecs describe-tasks \
            --cluster default \
            --tasks $TASK_ARN \
            --region ${{ env.AWS_REGION }} \
            --query 'tasks[0].containers[0].exitCode' \
            --output text)

          if [ "$EXIT_CODE" != "0" ]; then
            echo "❌ Migrations failed with exit code $EXIT_CODE"
            
            # Get logs for debugging
            echo "📋 Migration logs:"
            aws logs tail /ecs/cashsouk-migrate --since 10m --region ${{ env.AWS_REGION }} || true
            
            exit 1
          fi

          echo "✅ Migrations completed successfully"

      - name: Push and deploy API
        if: ${{ inputs.deploy_api }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: api-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "📦 Pushing API image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

          echo "📝 Updating task definition..."
          TASK_DEF=$(aws ecs describe-task-definition --task-definition cashsouk-api --region ${{ env.AWS_REGION }})
          NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
            '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
          aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null

          echo "🚀 Deploying API to ECS..."
          aws ecs update-service \
            --cluster default \
            --service api-cashsouk-09ff \
            --task-definition cashsouk-api \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ API deployed successfully"

      # Landing: Build → Push → Deploy (immediately)
      - name: Build push and deploy Landing
        if: ${{ inputs.deploy_landing }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: landing-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Landing image..."
          docker build -f docker/landing.Dockerfile \
            --build-arg NEXT_PUBLIC_API_URL=${{ secrets.NEXT_PUBLIC_API_URL }} \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Landing image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

          echo "📝 Updating task definition..."
          TASK_DEF=$(aws ecs describe-task-definition --task-definition cashsouk-landing --region ${{ env.AWS_REGION }} 2>/dev/null || echo "{}")
          if [ "$TASK_DEF" != "{}" ]; then
            NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
            aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null
          fi

          echo "🚀 Deploying Landing to ECS..."
          aws ecs update-service \
            --cluster default \
            --service landing-cashsouk-1156 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Landing deployed successfully"

      # Investor: Build → Push → Deploy (immediately)
      - name: Build push and deploy Investor
        if: ${{ inputs.deploy_investor }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: investor-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Investor image..."
          docker build -f docker/portal-investor.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Investor image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

          echo "📝 Updating task definition..."
          TASK_DEF=$(aws ecs describe-task-definition --task-definition cashsouk-investor --region ${{ env.AWS_REGION }} 2>/dev/null || echo "{}")
          if [ "$TASK_DEF" != "{}" ]; then
            NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
            aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null
          fi

          echo "🚀 Deploying Investor to ECS..."
          aws ecs update-service \
            --cluster default \
            --service investor-cashsouk-50e5 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Investor deployed successfully"

      # Issuer: Build → Push → Deploy (immediately)
      - name: Build push and deploy Issuer
        if: ${{ inputs.deploy_issuer }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: issuer-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Issuer image..."
          docker build -f docker/portal-issuer.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Issuer image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

          echo "📝 Updating task definition..."
          TASK_DEF=$(aws ecs describe-task-definition --task-definition cashsouk-issuer --region ${{ env.AWS_REGION }} 2>/dev/null || echo "{}")
          if [ "$TASK_DEF" != "{}" ]; then
            NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
            aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null
          fi

          echo "🚀 Deploying Issuer to ECS..."
          aws ecs update-service \
            --cluster default \
            --service issuer-cashsouk-ff47 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Issuer deployed successfully"

      # Admin: Build → Push → Deploy (immediately)
      - name: Build push and deploy Admin
        if: ${{ inputs.deploy_admin }}
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
          ECR_REPOSITORY: admin-cashsouk
          IMAGE_TAG: ${{ github.sha }}
        run: |
          echo "🔨 Building Admin image..."
          docker build -f docker/portal-admin.Dockerfile \
            -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .

          echo "📦 Pushing Admin image to ECR..."
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG || echo "⚠️ Image already exists, skipping push"

          echo "📝 Updating task definition..."
          TASK_DEF=$(aws ecs describe-task-definition --task-definition cashsouk-admin --region ${{ env.AWS_REGION }} 2>/dev/null || echo "{}")
          if [ "$TASK_DEF" != "{}" ]; then
            NEW_TASK_DEF=$(echo $TASK_DEF | jq --arg IMAGE "$ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG" \
              '.taskDefinition | .containerDefinitions[0].image = $IMAGE | del(.taskDefinitionArn, .revision, .status, .requiresAttributes, .compatibilities, .registeredAt, .registeredBy)')
            aws ecs register-task-definition --cli-input-json "$NEW_TASK_DEF" --region ${{ env.AWS_REGION }} > /dev/null
          fi

          echo "🚀 Deploying Admin to ECS..."
          aws ecs update-service \
            --cluster default \
            --service admin-cashsouk-7cd8 \
            --force-new-deployment \
            --region ${{ env.AWS_REGION }}

          echo "✅ Admin deployed successfully"

      - name: Deployment summary
        run: |
          echo "✅ Deployment completed"
          echo "Image tag: ${{ github.sha }}"
          echo ""
          echo "Services deployed:"
          if [ "${{ inputs.deploy_api }}" == "true" ]; then echo "  - API"; fi
          if [ "${{ inputs.deploy_landing }}" == "true" ]; then echo "  - Landing"; fi
          if [ "${{ inputs.deploy_investor }}" == "true" ]; then echo "  - Investor Portal"; fi
          if [ "${{ inputs.deploy_issuer }}" == "true" ]; then echo "  - Issuer Portal"; fi
          if [ "${{ inputs.deploy_admin }}" == "true" ]; then echo "  - Admin Portal"; fi
