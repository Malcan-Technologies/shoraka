# Audit & Notification Product Gap Review

> **Document responsibility:** this file owns **unresolved product/compliance gaps** and the
> **historical resolution record** for gaps that have since been closed. It answers *"what is still
> broken or awaiting product sign-off?"* It is deliberately **not** the place to look up what an
> event does or where it appears.
>
> | Question | Document |
> |---|---|
> | What happens for `EVENT_X`? | [`audit-event-surface-matrix.md`](./audit-event-surface-matrix.md) — **primary reference** |
> | Is the evidence we store good enough? | [`audit-event-catalog.md`](./audit-event-catalog.md) |
> | What should I *call* this on a new surface? | [`activity-notification-copy-standard.md`](./activity-notification-copy-standard.md) |
> | Why is it worded that way, and was it reviewed? | [`activity-notification-copy-review.md`](./activity-notification-copy-review.md) |
>
> **Reconciled against source 2026-08-24.** Several findings below were stale — either already fixed
> or never accurate. Rather than deleting them, each carries a strikethrough plus a
> **RESOLVED** / **CORRECTED** annotation so the history stays legible. Counts in §1 and §5 are
> superseded by [`audit-event-surface-matrix.md`](./audit-event-surface-matrix.md) §7.
>
> ⚠️ **Before acting on any finding here, re-verify against source with `rg`/`cat`.** Editor search
> indexes on this repository return phantom files under `apps/api/src/modules/*/audit/` that do not
> exist on disk and carry a different event vocabulary. See matrix §8.1.

This is a **findings-and-recommendations report**, not an implementation. It covers log completeness, correct representation of business actions, notification correctness, and compliance-evidence gaps across Admin, Issuer, and Investor portals, cross-checked against the *Cashsouk Issuer User Journey v2 ATS* compliance document.

Companion document: [`audit-event-catalog.md`](./audit-event-catalog.md) (developer reference for every live event/notification, by module).

**Change policy applied to this pass:** per the preservation-first contract that governs this codebase's audit system, findings below are only auto-implemented when they are (a) test/documentation defects, (b) low-risk missing evidence on an *existing* writer that adds no new UI-visible output, or (c) broken description/remark propagation. Everything else — wording changes, new notifications, reminder-cadence changes, sequencing/business-rule changes — is **reported for a product decision**, not silently fixed, because it changes user-visible behavior or compliance posture.

---

## 1. Executive summary

| Category | Count | Severity |
|---|---|---|
| Compliance sequence/requirement gaps vs. issuer journey PDF | 9 | **HIGH** — several are hard regulatory requirements (reminder cadence, fee timing, Notice of Assignment, guarantor acknowledgment) |
| Notification correctness issues (dead, misdirected, or missing) | 8 | MEDIUM–HIGH |
| Presentation/visibility mismatches (stored but hidden, or mislabeled) | ~~11~~ **1 still open (item 10 — product decision)** — 9 of the 11 have since been resolved or were found to be inaccurate, and 1 (item 8) was investigated and classified as intentional-by-design, needing no fix (see §4) | MEDIUM |
| Dead / non-live events (declared, never written, or unreachable) | ~~17 confirmed~~ ~~19 non-live values~~ ~~20 non-live values~~ ~~24 non-live values~~ **23 non-live values, using precise classifications** — see matrix §7.1. Includes `AML_APPROVED` (reclassified **UNREACHABLE** 2026-08-24), `access_logs.ROLE_ADDED`/`ROLE_REMOVED`/`ONBOARDING_RESET` + `onboarding_logs.ONBOARDING_RESET` (reclassified **UNREACHABLE** 2026-08-25), and `note_events.ISSUER_RESIDUAL_WITHDRAWAL_CREATED` (**removed from source 2026-08-25** — dead cleanup pass, see §4 item 12) | LOW (no user impact; the remaining values are either dormant plumbing with a real backend writer, or retained-for-compatibility artifacts investigated and confirmed unsafe to remove — see §4 item 12) |
| Data-quality / evidence gaps on live writers | 6 | LOW–MEDIUM |
| Fixed across both implementation passes | 10 (see the two "Follow-up implementation pass" notes above) | — |

**Bottom line:** the audit/logging *plumbing* (writers, tables, standard fields) built in the prior preservation-standardization phase is sound and well-tested. The gaps found in **this** phase are almost entirely **product/business-logic gaps that predate that work** — things origin/main never implemented, not things the standardization broke. The highest-priority items are the four compliance-sequence mismatches in §2, because they represent code behavior that contradicts a written regulatory requirement today, in production, regardless of any audit-logging work.

**Follow-up implementation pass (2026-08-24):** product subsequently approved and this repo now implements 5 of the previously-reported, non-compliance items: §3.1 `withdrawal_submitted_to_trustee` wiring; §4 items 1 (`COD_REJECTED` Activity visibility), 7 (issuer timeline milestone visibility), and 11 (dead onboarding filter cleanup); and §6 item 4 (`APPLICATION_RESUBMITTED` bare-path description). All nine §2 compliance-sequence gaps and every other §3/§4/§6 item are explicitly **left untouched** — see the inline strikethrough/RESOLVED annotations below for exactly what changed and why. Full BEFORE/DECISION/AFTER detail lives in `audit-event-catalog.md`.

**Second follow-up implementation pass (2026-08-24, same day, non-compliance items only):** implements 5 more previously-reported, non-compliance/non-sequence items: §4 items 3 (Admin Access-log filter) and 4 (Admin Security-log filter); a new Admin-org-detail-specific half of item 1 (`COD_REJECTED` was already visible to issuer/investor from the first pass, but the **Admin's own** organization-detail Activity/CSV query allowlist separately omitted it); a portal-copy fix for `ONBOARDING_CANCELLED` (was misleadingly worded as permanent); and item 9 (Note timeline/CSV 50-event cap — the export path is now unlimited while the UI stays paginated). It also completes a **documentation-only investigation pass** (no functional code changes) covering: `AML_APPROVED` reclassification from LIVE to **UNREACHABLE** (§3.2, §5, §6 item 2), `application_review_events` classification (§4 item 8), SigningCloud evidence-field support (§6 item 5), Product inactivate/reactivate reachability (§4 item 10, §5), gateway-capture semantic classification (§6 item 6), and a notification-policy classification pass (§3.2, §3.3). The nine §2 compliance-sequence gaps remain untouched, as does every §3/§4/§6 item not listed above. Counts in §1 and §5, and in `audit-event-surface-matrix.md` §7, are recalculated from source (not carried forward) — Live 142→141, Not-live 19→20, because `AML_APPROVED` moved from LIVE to UNREACHABLE.

**Third follow-up — documentation/comment-only reachability correction (2026-08-25):** the Admin Access-log filter fix from the second pass (§4 item 3) was re-traced from source, starting from `AdminService.updateUserRoles` and `AdminService.resetOnboarding` per explicit request, because "writer exists" had been used as a proxy for "live" without separately verifying an Admin UI path reaches each writer. Result: `ROLE_ADDED`, `ROLE_REMOVED`, and `access_logs`/`onboarding_logs` `ONBOARDING_RESET` are **reclassified LIVE → UNREACHABLE** (backend/API reachable, no current Admin UI caller); `PROFILE_UPDATED` was re-confirmed genuinely **LIVE_UI_REACHABLE**; all four `security_logs` filter additions from the same second pass (`ROLE_CREATED`, `ROLE_REMOVED`, `ROLE_PERMISSIONS_UPDATED`, `INVITATION_REVOKED`) were re-confirmed genuinely reachable via `admin-permission-configuration.tsx` / `app/settings/roles/page.tsx` and needed no change. **No code, filter, route, or hook was changed** — `ACCESS_EVENT_TYPES` keeps all four values so no stored/API-created row is silently hidden; only the reachability classification and the surrounding comment/documentation were corrected. See §4 item 3, §5, `audit-event-catalog.md` §1.1/§1.4, and `audit-event-surface-matrix.md` §2.1/§9 #12. Counts in §1 and §5 recalculated again — Live 141→137, Not-live 20→24.

---

## 2. Compliance sequence gaps (issuer journey PDF cross-check)

These were verified by direct code inspection, not by the writer/catalog audit — they are business-logic findings, but they surfaced *because* this audit review was matching each event's evidence against a written requirement.

| # | Requirement (issuer journey PDF) | Code reality | Evidence | Classification |
|---|---|---|---|---|
| 1 | LO acceptance reminders at **day 3 and day 6** of a 7-day clock | Only **one** reminder, at day 6 (`days_before_expiry: 1`) | `packages/types/src/deadline-config.ts:25-28` | **WRONG_TIMING** |
| 2 | Signing reminders at **day 7 and day 12** of a 14-day clock | Reminders at **day 11 and day 13** (`days_before_expiry: 3` and `1`) | `packages/types/src/deadline-config.ts:30-33` | **WRONG_TIMING** |
| 3 | Onboarding fee charged **after** AML approval | Company issuer fee is charged **before** eKYC/AML (`terms → fee → verify`) | `packages/config/src/onboarding-flow.ts:114-135` | **WRONG_SEQUENCE** |
| 4 | Notice of Assignment + written paymaster acknowledgment **before disbursement** | No such gate anywhere in code; searched application/signing/disbursement modules | Repo-wide search, zero matches | **MISSING** |
| 5 | Guarantee Acknowledgment paired with Letter of Offer at issuance | Guarantors are only contacted at the **signing** phase, not at offer/LO issue; no acknowledgment-tracking entity | `admin/service.ts:sendContractOffer` (issuer-only notify); `signing/service.ts` (signing-phase emails) | **MISSING** |
| 6 | Risk Statement as an active, exportable self-declaration form | Checkbox + PDF-open only; acceptance row has no risk-questionnaire payload | `legal-documents.ts` (`RISK_STATEMENT` wording); `acceptance-service.ts` | **MISSING** |
| 7 | Warning Statement shown at signup, on every new financing application, and permanently visible; each display logged | Signup capture only; compact portal footer omits the Warning link; no per-application display/acknowledgment log | `legal-documents.ts:89-96` (footer list); `compact-portal-legal-links.ts:18-25` | **PARTIAL** |
| 8 | T&C publication gated by Securities Commission clearance | Draft/publish + hash + optional re-acceptance exists; no SC-clearance field or gate | `legal-documents/schemas.ts:publishVersionSchema` (only has `reacceptanceRequired`) | **PARTIAL** |
| 9 | Sale Contract profit rate capped at 18% p.a. | No cap validation; schema allows any non-negative rate | `notes/schemas.ts:133` (`profitRatePercent: min(0)`) | **MISSING** |

**Not a gap — confirmed working:** the 80%-minimum-funding rule and investor refund-on-failure path are fully implemented (`closeFunding`/`failFunding` + `creditInvestorBalance`), and money movements are correctly segregated into ledger buckets (`INVESTOR_POOL`, `REPAYMENT_POOL`, `ISSUER_PAYABLE`, `OPERATING_ACCOUNT`) even though there is no single schema-level "trust account" entity.

**Recommended next step:** items 1, 2, and 3 are the most concrete and lowest-effort to fix (they're config/sequencing changes, not new features) but **all three change user-facing timing/flow behavior**, so they need explicit product sign-off before implementation — not because the fix is technically hard, but because changing when a fee is charged or when a reminder fires is a business decision, not a logging decision. Items 4–9 are larger product gaps (missing entities/workflows) that should go on a backlog, prioritized by legal/compliance review.

---

## 3. Notification correctness issues

### 3.1 Dead notification types (registered, zero automatic trigger)

Confirmed via exhaustive `sendTyped`/`sendTypedPlatformOnly` call-site search across `apps/api/src`:

| Type ID | Why it matters | Classification |
|---|---|---|
| `login_new_device` | Template exists ("Login from New Device") but no device-fingerprinting/new-device-detection code calls it | MISSING_REQUIRED_NOTIFICATION (if still wanted) |
| `kyc_approved` / `kyc_rejected` | Identity-verification decisions are communicated via `onboarding_approved`/`onboarding_rejected` instead; these two are pure leftovers | DEAD — candidate for registry cleanup, not a coverage gap |
| `application_approved` | No `APPLICATION_APPROVED` log writer either (see catalog §2.1); `application_completed` is the real terminal signal | DEAD — candidate for registry cleanup |
| ~~`withdrawal_submitted_to_trustee`~~ | ~~Audit event **is** logged (`WITHDRAWAL_SUBMITTED_TO_TRUSTEE` in `note_events`); the user-facing notification for the same moment is simply never sent~~ | ~~**MISSING_REQUIRED_NOTIFICATION**~~ **RESOLVED (2026-08-24)** — see recommendation below |
| `system_announcement` / `new_product_alert` | Never triggered via `sendTyped*`; only reachable through the admin's manual bulk-broadcast tool, which bypasses these registry templates entirely and uses admin-typed copy instead | DEAD as automatic triggers — registry entries are effectively unused templates |

**Recommendation:** `withdrawal_submitted_to_trustee` is the one item here that looks like an unintentional gap rather than a stale registry entry — the audit trail proves the business moment fires in production, but the corresponding user notification was apparently never wired up. This is a **product decision** on whether issuers should be told when their withdrawal is submitted to the trustee (most likely yes), but wiring it is a one-line `sendTyped` call once approved — flagging for product sign-off rather than implementing silently, since it changes what issuers see in their inbox.

**RESOLVED (2026-08-24):** BEFORE — as described above, no `sendTyped*` call site existed. DECISION — approved; issuers should be told (informational only). AFTER — `notes/service.ts:markWithdrawalSubmitted` now calls `notifyWithdrawalSubmittedToTrustee()` (in `note-lifecycle-notifications.ts`) right after the existing `WITHDRAWAL_SUBMITTED_TO_TRUSTEE` audit-event write, reusing the established `sendToIssuerOrg` recipient helper (issuer org owner + members, platform-only channel, same pattern as other note-lifecycle notifications). No new notification type, no channel/preference/workflow/status/timing change.

### 3.2 Missing notifications for business decisions that currently only log

| Business moment | Currently | Classification |
|---|---|---|
| Admin approves onboarding submission (pre-AML gate) | `ONBOARDING_APPROVED` logged, no notification | PRODUCT_DECISION_REQUIRED |
| ~~Admin approves AML screening~~ Live AML clearance (automatic, provider-driven) | ~~`AML_APPROVED` logged, no notification~~ **CORRECTED (2026-08-24):** `AML_APPROVED` is **UNREACHABLE** — it is never actually written (see §5, §6 item 2, and `audit-event-surface-matrix.md` §2.3). The real, live AML milestone is `ONBOARDING_STATUS_UPDATED` + `metadata.amlApproved:true`, written automatically from RegTank results; it has no notification either | PRODUCT_DECISION_REQUIRED |
| Admin approves SSM/CTOS verification | `SSM_APPROVED` logged, no notification | PRODUCT_DECISION_REQUIRED |
| Application submitted (issuer's own confirmation) | `APPLICATION_SUBMITTED` logged, no confirmation notification | PRODUCT_DECISION_REQUIRED |
| Issuer full repayment / note payoff to issuer per-payment | Only `note_repaid_issuer` on full payoff; no partial-payment notice to issuer — **asymmetric with the investor side**, which already gets `note_payment_received` per payment | LIKELY_MISSING_NOTIFICATION |
| Gateway refund outcomes (`REFUND_INITIATED`/`REFUNDED`) | No notification type exists — the depositor is never told their deposit was refunded | LIKELY_MISSING_NOTIFICATION |
| Gateway deposit name-check outcomes (`NAME_CHECK`/`_APPROVED`/`_REJECTED`) | No notification type exists — the depositor is never told their deposit is held pending manual review | NEEDS_POLICY_CONFIRMATION (could be intentionally silent while admin resolves, to avoid alarming the user pre-resolution; could also be a real gap) |

**Reclassified 2026-08-24** using the requested policy taxonomy (`INTENTIONAL_SILENT` / `PRODUCT_DECISION_REQUIRED` / `LIKELY_MISSING_NOTIFICATION` / `INTENTIONAL_RECIPIENT_SCOPE` / `NEEDS_POLICY_CONFIRMATION`), documentation only — **no notifications were added**. The four intermediate-admin-gate rows (`ONBOARDING_APPROVED`, the live AML milestone, `SSM_APPROVED`, `APPLICATION_SUBMITTED`) all precede a later, larger milestone that *is* notified (e.g., AML clearance precedes final activation, which is notified) — this is a consistent, repeated pattern in the code, but there is no source comment or product record confirming it was a deliberate silence decision rather than an oversight, so `PRODUCT_DECISION_REQUIRED` (not `INTENTIONAL_SILENT`) is the honest classification until product confirms. The partial-repayment and refund gaps are asymmetric with an already-notified counterpart action, which is why they're classified as more likely genuine gaps rather than open questions.

### 3.3 Recipient-scope inconsistency (not wrong, but worth knowing)

Three different, independently-intentional recipient patterns exist across domains for what is conceptually the same "who represents this org" question:

| Domain | Recipients | Classification |
|---|---|---|
| Application/offer notifications | Org owner + `OWNER`/`ORGANIZATION_ADMIN` members | **INTENTIONAL_RECIPIENT_SCOPE** — dedicated helper `getIssuerRecipientUserIdsForApplication` |
| Note-lifecycle notifications | Org owner + **all** org members (broadest) | **INTENTIONAL_RECIPIENT_SCOPE** — dedicated helpers `listIssuerOrgMemberUserIds` / `listInvestorOrgMemberUserIds` |
| Director/shareholder action-required notifications | Org owner **only** (narrowest) | **INTENTIONAL_RECIPIENT_SCOPE** — dedicated module `director-shareholder-notifications.ts` |

Each pattern is backed by its own dedicated, purpose-built recipient-resolution helper (see `audit-event-catalog.md` §5.3) — this is evidence of deliberate per-domain design, not an accident of one shared helper being reused inconsistently. None of these is "wrong" in isolation, but the same organization will see different notification audiences depending on which domain fired the event. This is worth product awareness, not a code defect — **do not normalize the three recipient scopes into one; no action taken.**

### 3.4 Reminder-copy gap

`offer_expiry_reminder_24h` is used for **every** configured reminder day (not just 24h before expiry) — its name is misleading internally but has no user-facing impact since the copy itself says "expires in N days." Invoice-offer expiry/reminder payloads omit `invoiceNumber`, so invoice reminder copy is generically worded where contract reminders include the contract number. Low-priority copy improvement, reported not fixed.

### 3.5 Low-priority copy/cleanup review (documentation only, 2026-08-24)

Consolidated list of low-priority, cosmetic-only items reviewed this pass — **no constants were
renamed or deleted**:

| Item | Finding | Action |
|---|---|---|
| `offer_expiry_reminder_24h` internal name | Misleading — used for every configured reminder offset, not just 24h (see §3.4 above) | Documented, not renamed — renaming a registry type id is a breaking change to historical `notifications` rows |
| Invoice offer reminder payload | Omits `invoiceNumber`, unlike the contract equivalent which includes the contract number (see §3.4 / catalog §2.7) | Documented, not fixed — cosmetic copy-completeness gap only |
| Dead notification types: `kyc_approved`, `kyc_rejected`, `login_new_device`, `application_approved` | Confirmed zero `sendTyped`/`sendTypedPlatformOnly` call sites anywhere in `apps/api/src` (§3.1) | Documented as cleanup candidates; not deleted — removing a `notification_types` row is a schema-adjacent, breaking change for any historical row referencing it |
| Dead/seed/dev/unreachable event values | Full enumeration already lives in §5 above and in `audit-event-surface-matrix.md` §7.1 (the authoritative count, now 23 non-live values after the `AML_APPROVED` (2026-08-24), `ROLE_ADDED`/`ROLE_REMOVED`/`ONBOARDING_RESET` (2026-08-25) reclassifications, and the `ISSUER_RESIDUAL_WITHDRAWAL_CREATED` removal (2026-08-25, §4 item 12)) | Cross-referenced, not duplicated here |

No code changes were made for any item in this subsection.

---

## 4. Presentation / visibility mismatches

| # | Finding | Where | Classification |
|---|---|---|---|
| 1 | ~~`COD_REJECTED` (corporate onboarding rejection) is excluded from the issuer/investor Activity feed allowlist, even though the `ONBOARDING_REJECTED` notification **is** sent for the same rejection~~ **RESOLVED (2026-08-24)** — added to `getEventTypes()` with canonical "Onboarding Rejected" copy; see `audit-event-catalog.md` §1.4 for BEFORE/DECISION/AFTER | `OrganizationLogAdapter.getEventTypes()` | VISIBILITY_MISMATCH — user gets a notification pointing at an event that then doesn't appear in their own activity history |
| 2 | ~~`ONBOARDING_APPROVED` and `FINAL_APPROVAL_COMPLETED` share the identical portal title "Onboarding Approved"~~ **CORRECTED (2026-08-24)** — the finding is stale. BEFORE: reported as identical. DECISION: re-verify in source. AFTER: `organization-log.ts:216–225` gives them **distinct** titles and descriptions — `ONBOARDING_APPROVED` → "Onboarding Submission Approved", `FINAL_APPROVAL_COMPLETED` → "Onboarding Approved". No action required | ~~`organization-log.ts:211-216`~~ `organization-log.ts:216-225` | ~~MISLEADING~~ **NOT A DEFECT** |
| 3 | ~~Admin Access-log filter dropdown lists `KYC_STATUS_UPDATED` (never written) but omits live `ROLE_ADDED`, `ROLE_REMOVED`, `PROFILE_UPDATED`, `ONBOARDING_RESET`~~ **RESOLVED (2026-08-24), reachability corrected (2026-08-25)**. BEFORE — `ACCESS_EVENT_TYPES` omitted four `access_logs` event types that had a real production writer function, while still offering the never-written `KYC_STATUS_UPDATED`. PASS A (2026-08-24) — the four were added to `ACCESS_EVENT_TYPES` (moved into `use-access-logs.ts`, exported for testing) so any stored or API-created row of that type would not be silently hidden from the query/CSV; `KYC_STATUS_UPDATED` was dropped from the filter; `EventType` in `packages/types/src/admin.ts` gained `ROLE_REMOVED`/`ONBOARDING_RESET`. Query/filter only, no writer or enum removal. FOLLOW-UP TRACE (2026-08-25, starting from `AdminService.updateUserRoles`/`resetOnboarding` per explicit request) — of the four, only `PROFILE_UPDATED` is actually reachable through the current Admin UI (`useUpdateUserProfile` → `user-account-profile-panel.tsx`). `ROLE_ADDED` and `ROLE_REMOVED` have a full route→SDK→hook chain (`useUpdateUserRoles`) but **zero `.tsx` callers**; `ONBOARDING_RESET` has a route but no SDK method, hook, or caller at all (documented in its own Swagger comment as "temporary feature for testing"). None of the three is reachable through normal admin button/form usage today. AFTER — the filter values are unchanged (this is not reopened as a defect: the allowlist's job is to not hide any row the API can write, which still holds); `audit-event-catalog.md` §1.1/§1.4 and `audit-event-surface-matrix.md` §2.1/§9 #12 now document `ROLE_ADDED`/`ROLE_REMOVED`/`ONBOARDING_RESET` as **UNREACHABLE** rather than LIVE | `use-access-logs.ts:15-38` | ~~VISIBILITY_MISMATCH~~ **RESOLVED** (filter); reachability **reclassified**, not reopened |
| 4 | ~~Admin Security-log filter shows 5 types; `ROLE_CREATED`, `ROLE_REMOVED`, `ROLE_PERMISSIONS_UPDATED`, `INVITATION_REVOKED` are stored but excluded~~ **RESOLVED (2026-08-24)** — `SECURITY_EVENT_TYPES` (moved into `use-security-logs.ts`, exported for testing) now includes all nine live `security_logs` writers. `SecurityEventType` in `packages/types/src/admin.ts` gained the four missing members. Confirmed `security_logs.ROLE_REMOVED` (role-catalogue delete) and `access_logs.ROLE_REMOVED` (user role removal) remain distinct, unmerged strings | `use-security-logs.ts:17-27` | ~~VISIBILITY_MISMATCH~~ **RESOLVED** |
| 5 | ~~`CONTRACT_WITHDRAWN` (fired when an **issuer rejects** an offer) is labeled **"Facility Offer Withdrawn"** in the admin timeline and CSV — identical wording to `CONTRACT_OFFER_RETRACTED`~~ **RESOLVED (2026-08-24)**. BEFORE: as described; the parenthetical also claimed the issuer timeline said "Facility withdrawn". DECISION: relabel so the admin surfaces state who acted. AFTER, verified in source: admin timeline `CONTRACT_WITHDRAWN: "Facility Offer Rejected"` (`admin-activity-timeline.tsx:149`) vs `CONTRACT_OFFER_RETRACTED: "Facility Offer Retracted"` (:145); CSV `"Facility offer rejected"` (`contract-activity-csv.ts:35`) vs `"Facility offer retracted"` (:31). Issuer copy is **"You declined the facility offer"** (`application-timeline.ts:33`), not "Facility withdrawn". The confusing `"Facility Offer Withdrawn"` string now sits only on the **dead** `CONTRACT_OFFER_REJECTED`, which no writer can produce | `admin-activity-timeline.tsx:144-149`; `contract-activity-csv.ts:30-35` | ~~MISLEADING~~ **RESOLVED** |
| 6 | ~~`AMENDMENTS_SUBMITTED` issuer-facing label reads **"You submitted requested changes"** — but the writer is the **admin** sending amendment requests **to** the issuer~~ **RESOLVED (2026-08-24)**. BEFORE: as described. DECISION: reword so the direction of action is correct. AFTER, verified in source: **both** issuer surfaces now read **"Changes requested"** (`facility-transactions.ts:52`, `application-timeline.ts:44`), the issuer activity feed reads "Changes Requested" / "We need updates to your application before it can continue." (`application-log.ts:602`), admin reads "Amendment Request Sent" and CSV reads "Amendment request sent". The misleading *enum name* remains — it is documented as a permanent naming trap in matrix §8.2, not renamed | `facility-transactions.ts:52`; `application-timeline.ts:44` | ~~MISLEADING~~ **RESOLVED (copy)**; enum name intentionally left alone |
| 7 | ~~Issuer timeline `EVENT_LABELS` map still keys off dead `OFFER_EXPIRED`/`CONTRACT_OFFER_REJECTED` instead of the live `CONTRACT_OFFER_EXPIRED`/`CONTRACT_WITHDRAWN` — those milestones fall through to a generic label or don't render~~ **RESOLVED** — `CONTRACT_WITHDRAWN` relabeled in the prior copy-consistency pass; `CONTRACT_OFFER_EXPIRED` (plus the other 7 approved milestones, contract + invoice) added to the label map on 2026-08-24, see `audit-event-catalog.md` §2.2 | `application-timeline.ts:27-35` | VISIBILITY_MISMATCH / GENERIC_FALLBACK |
| 8 | `application_review_events` table has no production reader at all (confirmed: `admin/repository.ts` never queries it; `RecentActivityCard` ignores its `events` prop when `applicationId` is set) | catalog §2.5 | **Investigated 2026-08-24** — classified **INTENTIONAL_FORENSIC_ONLY**. The writer module's own header comment documents the design intent: an in-transaction forensic mirror of `application_logs`, kept "in case a reader is added later." Not a redundant/accidental table — no action needed, no reader added |
| 8b *(admin-side of item 1)* | ~~`COD_REJECTED` was also excluded from the **Admin** organization-detail Activity query allowlist (`use-organization-logs.ts`'s `ONBOARDING_EVENT_TYPES`) — a separate gap from the issuer/investor-facing one closed in the first pass, since admin org-detail uses its own allowlist~~ **RESOLVED (2026-08-24)** — added to `ONBOARDING_EVENT_TYPES`, with the `"Onboarding Rejected"` label added to `organization-activity-timeline.tsx`; the org-detail CSV export shares the same query and now includes it too | `use-organization-logs.ts` | ~~VISIBILITY_MISMATCH~~ **RESOLVED** |
| 8c | ~~Issuer/investor portal copy for `ONBOARDING_CANCELLED` read "...was cancelled and will not continue," implying permanent termination, when the actual action is an admin-initiated restart~~ **RESOLVED (2026-08-24)** — title changed to `"Onboarding Restarted"`, description to `"Your previous onboarding request was cancelled and a new onboarding request has been started."` Stored `event_type`, trigger, workflow, and notification behavior unchanged; Admin's own forensic wording untouched | `organization-log.ts` | ~~MISLEADING~~ **RESOLVED (copy only)** |
| 9 | ~~Admin note timeline **and** its CSV export are hard-capped at 50 events (`take: 50`), with no unlimited compliance-export path~~ **RESOLVED (2026-08-24)** — added `NoteRepository.findAllEventsByNoteId` (no `take` limit) and switched `NoteService.listEvents` (the export data source) to use it; extracted `mapNoteEventRecords` so the UI timeline and the export share identical mapping/ordering logic. The **visible UI timeline stays capped at 50** for performance; only the export/audit path is now unlimited | `noteInclude.events`; `NoteTimelinePanel`; `notes/service.ts:listEvents` | ~~MEDIUM~~ **RESOLVED** |
| 10 | ~~Product-log admin panel badges/filters only style `PRODUCT_CREATED`/`_UPDATED`/`_DELETED`; `_INACTIVATED`/`_REACTIVATED` fall back to a raw badge~~ **CORRECTED (2026-08-24)** — the finding was inaccurate. All **five** types have a label and a colour (`product-logs-panel.tsx:39-43`). The real, more serious issue is the inverse: `PRODUCT_INACTIVATED` and `PRODUCT_REACTIVATED` are **UNREACHABLE** — their writers (`setInactive()`, `restoreProduct()`) have zero callers, so the panel offers two filter options that can never return a row. **Re-investigated 2026-08-24**: confirmed zero callers of either method anywhere in the repository (not just outside the repository class), no alternate product-status-change mechanism exists, and the Admin UI has no activate/deactivate action anywhere else (only `PRODUCT_DELETED`, a different action, is reachable). Recommendation options: (a) wire the two methods to a real Admin UI action, (b) remove the two dead filter options and keep the writers dormant, or (c) leave everything as-is (lowest priority — cosmetic-only impact, since the filter simply never returns rows). No code change made in this pass — this needs an explicit product decision, and "no product activate/deactivate wiring" is an explicit non-goal of this pass | `product-logs-panel.tsx:39-43`; `repository.ts:setInactive`(~679)/`restoreProduct`(~713) | ~~GENERIC_FALLBACK~~ **UNREACHABLE_WRITER** — still open, cosmetic impact only |
| 11 | ~~Legacy dead event types (`TNC_ACCEPTED`, `KYC_APPROVED`, `KYB_APPROVED`) still appear as filter options in the admin org-log dropdown despite never being written~~ **RESOLVED (2026-08-24)** — removed from the `ONBOARDING_EVENT_TYPES` query-inclusion array after reconfirming zero production writers; enum values and historical rows untouched | `use-organization-logs.ts` filter list | VISIBILITY_MISMATCH — dropdown offers options that always return zero results |
| 12 | **DEAD/SEED_ONLY cleanup pass (2026-08-25)** — 14 candidates re-verified from source (not from prior docs) for safe removal: 3 `SEED_ONLY` (`access_logs.KYC_STATUS_UPDATED`; `onboarding_logs.TNC_ACCEPTED`/`KYC_APPROVED`), 7 `DEAD` audit events (`application_logs.APPLICATION_APPROVED`/`CONTRACT_OFFER_REJECTED`; `note_events.ISSUER_RESIDUAL_WITHDRAWAL_CREATED`; `gateway_payment_events.OVERRIDE_PROPOSED`/`OVERRIDE_APPROVED`/`OVERRIDE_REJECTED`; `onboarding_logs.KYB_APPROVED` — corrected from `SEED_ONLY`, zero occurrences even in `seed.ts`), and 4 `DEAD` notification types (`kyc_approved`, `kyc_rejected`, `login_new_device`, `application_approved`). **RESULT: 1 of 14 removed, 13 retained** — see `audit-event-surface-matrix.md` §9 #13 for the cross-document summary, and `audit-event-catalog.md` §1.1 (access/onboarding), §2.1 (application), §3.1 (note_events), §3.3 (gateway), and §5 (notifications) for the per-item catalog lines updated by this pass. Summary: (a) **REMOVED** — the single dead entry `"ISSUER_RESIDUAL_WITHDRAWAL_CREATED"` in `admin-note-events-sorting.ts`'s lifecycle-priority array (zero writers anywhere in `apps/api/src`; the key was never looked up since no row can ever have that `event_type`; `presentation-baseline.json` regenerated, all tests green). (b) **RETAINED** — the 3 `TNC_ACCEPTED`/`KYC_APPROVED`/`KYB_APPROVED` + `KYC_STATUS_UPDATED` label/dropdown-option code: already excluded from the default admin query allowlists (item 11 above, item 3 above); the remaining label-map entries and specific-value dropdown options exist specifically to render/filter `seed.ts`'s actual historical seed rows, and the fallback label logic already degrades gracefully — removing them would only make existing seed data render worse, with no live-code risk to mitigate. **Correction (2026-08-25, re-verified):** `KYB_APPROVED` has **zero** occurrences in `seed.ts` — unlike `KYC_STATUS_UPDATED`/`TNC_ACCEPTED`/`KYC_APPROVED`, it was never written even in dev seed data, so its correct classification is `DEAD` (declared-but-unused enum member), not `SEED_ONLY`; the registry's `"writer": "apps/api/prisma/seed.ts"` for `KYB_APPROVED` was stale and has been corrected. The retain decision is unchanged (same label-map/dropdown code, same "no safety benefit to removing" reasoning), only the classification label. (c) **RETAINED** — `APPLICATION_APPROVED`/`CONTRACT_OFFER_REJECTED`: `apps/api/src/lib/audit/preservation.test.ts` ("keeps at least a source reference for every application log event that had one") explicitly asserts at least one string reference survives in `apps/api/src` for every declared `application_logs` event type not in its `KNOWN_UNWRITTEN_EVENTS` allowlist, and its own comment states these two are intentionally excluded from that allowlist "because a reader/label reference to them already exists and must keep existing" — removing the reference would fail an existing regression test and reverses a previously-recorded design decision. (d) **RETAINED** — `OVERRIDE_PROPOSED`/`OVERRIDE_APPROVED`/`OVERRIDE_REJECTED`: confirmed these are real Prisma **enum** members of `GatewayPaymentEventType` (schema-level, not a plain string column like the other audit tables) — removing them is a migration, and "NO destructive database migration" is a hard rule for this pass. The surviving read path (`getOpenOverrideProposal()`) and copy-only labels indicate a partially-built override-workflow feature that was paused before a writer was added, not abandoned cruft; removing only the app-level copy while the enum/read-path remain would create inconsistency for no safety benefit. (e) **RETAINED** — the 4 dead notification types: `notification_types` is a real DB table with `onDelete: Cascade` foreign keys from `notifications`/`user_notification_preferences` (`notification_logs`'s FK has no explicit `onDelete`, i.e. `RESTRICT`) (`schema.prisma:1972-2078`); deleting the row (or the `seed-data.ts`/`registry.ts` entries that keep it in sync) risks orphaning or cascade-deleting historical notification data, which is exactly the destructive-history risk this pass must avoid — confirms the pre-existing reasoning in §3 above. **Corrected 2026-08-25** (traced `packages/ui/src/components/notification-preferences.tsx:64-66`): none of the four appear as end-user preferences in Account settings — that page only renders `user_configurable && category === "MARKETING"`, and all four are `SYSTEM`/`AUTHENTICATION`. They **do** all appear as global platform/email toggles in **Admin → Settings → Notifications → Configuration** (`apps/admin/src/app/settings/notifications/page.tsx:445-449` lists every seeded type in the `SYSTEM`/`AUTHENTICATION` categories, independent of `user_configurable`) — a genuine minor UX inconsistency (an admin-facing toggle for a notification that can never fire), but changing what's shown there is a product decision, not a zero-risk code cleanup, so it is reported here rather than changed. (f) **RETAINED, not evaluated for removal (out of scope)** — `USER_COMPLETED`: `RegTankDevWebhookHandler` (`webhook-handler-dev.ts`) is a live-registered route (`POST /v1/webhooks/regtank/dev/...`) that intentionally lets developers exercise RegTank webhooks against a separate dev-only Prisma client (`DATABASE_URL_DEV`) without touching production data or needing RegTank sandbox access — an active testing utility, not obsolete code, so left untouched per this pass's explicit instruction | `admin-note-events-sorting.ts` (removed); `preservation.test.ts`; `schema.prisma:2272-2284` (`GatewayPaymentEventType` enum); `schema.prisma:1972-2078` (`NotificationType`/`Notification`/`NotificationLog`); `seed-data.ts`; `webhook-handler-dev.ts` | 1 **CODE_REMOVED**; 13 **RETAINED_FOR_COMPATIBILITY** or **NOT_SAFE_TO_REMOVE** — no runtime business behavior changed |

**Items 1, 7, and 11 were approved and implemented on 2026-08-24**; **items 5 and 6 were also
resolved** by the copy-consistency pass; **items 3, 4, 8b, 8c, and 9 were approved and implemented
in the second same-day pass**; **items 2 and 10 were found to be inaccurate / re-investigated** and
corrected above; **item 8 was investigated and classified as intentional-by-design**; **item 12 (the
2026-08-25 dead/seed-only cleanup pass) removed one confirmed-dead array entry and retained the other
13 candidates for the source-verified reasons listed in that row**. That leaves only **item 10
open**:

| Still open | Nature | Why it was not silently fixed |
|---|---|---|
| 10 *(reclassified)* | `PRODUCT_INACTIVATED` / `PRODUCT_REACTIVATED` writers are unreachable | Either wire the callers or remove the filter options — a product decision either way; wiring a new admin action is an explicit non-goal of the 2026-08-24 cleanup pass |

See `audit-event-catalog.md` for full BEFORE/DECISION/AFTER detail on the implemented items.

---

## 5. Dead events (confirmed, no action needed)

These were already largely known from the preservation inventory. **Re-verified 2026-08-24** — two
of the previously-reported entries turned out not to be real events at all.

| Table | Non-live values | Status |
|---|---|---|
| `access_logs` | `ROLE_SWITCHED`, `ONBOARDING`, `USER_COMPLETED`, `ONBOARDING_STATUS_UPDATED`, `PASSWORD_CHANGED`, `EMAIL_CHANGED` (6 — each has a live equivalent in a *different* table) | Confirmed |
| `access_logs` | `KYC_STATUS_UPDATED` | Reclassified **SEED_ONLY** (written by `seed.ts`, so not strictly "never written") |
| `access_logs` (**reclassified 2026-08-25**) | `ROLE_ADDED`, `ROLE_REMOVED` (2) | **UNREACHABLE** — was previously catalogued LIVE. Re-traced `AdminService.updateUserRoles` from source: route (`PATCH /v1/admin/users/:id/roles`), SDK method, and `useUpdateUserRoles()` hook all exist, but **zero `.tsx` components call the hook**, and no other Admin UI path reaches this route. `ROLE_ADDED` is also not literally "a role was added" — it's the fallback branch for any outcome that doesn't strip `ADMIN`; `ROLE_REMOVED` fires only when `ADMIN` specifically is stripped. Not merged into the plain "dead" row above because the backend path is fully wired end-to-end — see §4 item 3 and §6 item 2-style framing |
| `access_logs` (**reclassified 2026-08-25**) | `ONBOARDING_RESET` | **UNREACHABLE** — was previously catalogued LIVE. `AdminService.resetOnboarding` has a route (its own Swagger comment calls it "temporary feature for testing") but **no SDK method, no hook, no `.tsx` caller at all** — one tier more unreachable than `ROLE_ADDED`/`ROLE_REMOVED`. Same reclassification applies to the `onboarding_logs.ONBOARDING_RESET` row below, since it's the same writer |
| `onboarding_logs` | `TNC_ACCEPTED`, `KYC_APPROVED` (2) | Reclassified **SEED_ONLY**; removed from the admin filter list on 2026-08-24, enum/rows untouched — see §4 item 11 |
| `onboarding_logs` | `KYB_APPROVED` (1) | **DEAD** (corrected 2026-08-25 from `SEED_ONLY` — zero occurrences in `seed.ts` too, unlike the two above); removed from the admin filter list on 2026-08-24 alongside them, enum untouched — see §4 items 11–12 |
| `onboarding_logs` | `USER_COMPLETED` | Reclassified **DEV_ONLY** — the sole writer is `regtank/webhook-handler-dev.ts` (~492), which targets `DATABASE_URL_DEV` |
| `onboarding_logs` (**reclassified 2026-08-24**) | `AML_APPROVED` | **UNREACHABLE** — was previously catalogued LIVE. Re-traced the full chain: route (`POST /v1/admin/onboarding-applications/:id/approve-aml`), service (`approveAmlScreening`), SDK method, and `useApproveAmlScreening` hook all exist, but **zero `.tsx` components call the hook**. It is designed manual-override plumbing, not the live AML mechanism — see §3.2 and §6 item 2. Not "dead" (the backend path is fully wired end-to-end and one UI change could reach it), hence the separate classification |
| `onboarding_logs` (**reclassified 2026-08-25**) | `ONBOARDING_RESET` | **UNREACHABLE** — same `AdminService.resetOnboarding` writer as the `access_logs` row above (route-only, no SDK/hook/UI caller); was previously catalogued LIVE |
| ~~`onboarding_logs` (**new find**)~~ | ~~`DIRECTOR_KYC_STATUS_UPDATED` — has a writer module (`director-kyc-outcomes.ts`) but zero importers anywhere~~ | **WITHDRAWN (2026-08-24)** — `rg` returns **zero** occurrences of that string anywhere in the repository, and `director-kyc-outcomes.ts` does not exist. Reclassified `NOT_AN_ACTUAL_EVENT`; almost certainly a phantom search-index hit. Director/shareholder outcomes are recorded as `EOD_APPROVED` / `EOD_REJECTED` / `EOD_WEBHOOK` |
| `application_logs` | `APPLICATION_APPROVED`, `CONTRACT_OFFER_REJECTED` (2) | Confirmed **DEAD** |
| `note_events` | ~~`ISSUER_RESIDUAL_WITHDRAWAL_CREATED` (1)~~ | **REMOVED (2026-08-25)** — confirmed dead (zero writers anywhere in `apps/api/src`) and safe to remove: it existed only as one entry in the `admin-note-events-sorting.ts` lifecycle-priority array, never looked up because no row can have that `event_type`. Deleted from that array (and the mirroring list in `docs/ai-context/admin-note-activity-timeline.md`); `presentation-baseline.json` regenerated; no schema/enum change (the column is a plain `String`, not a DB enum) — see §4 item 12 |
| `gateway_payment_events` | `OVERRIDE_PROPOSED`, `OVERRIDE_APPROVED`, `OVERRIDE_REJECTED` (3) | Confirmed **DEAD** |
| `product_logs` | `PRODUCT_INACTIVATED`, `PRODUCT_REACTIVATED` (2) | **UNREACHABLE** — writers exist, zero callers. Newly classified; see §4 item 10 |
| ~~`legal-documents` (test-fixture only)~~ | ~~`BOARD_RESOLUTION_UPLOADED`, `BOARD_RESOLUTION_REMOVED` — never referenced outside `cutover.test.ts`~~ | **WITHDRAWN (2026-08-24)** — `cutover.test.ts` does not exist on disk and both strings have zero occurrences. Phantom index hits from an unmerged branch; see matrix §8.1 |

~~**Total: 17 confirmed dead events/actions**~~ ~~**Total: 19 non-live values across the eight
stores**~~ ~~**Total: 20 non-live values across the eight stores**~~ ~~**Total: 24 non-live values
across the eight stores**~~ **Total: 23 non-live values across the eight stores** (2026-08-24:
`AML_APPROVED` reclassified LIVE → UNREACHABLE, +1; 2026-08-25: `access_logs.ROLE_ADDED`,
`access_logs.ROLE_REMOVED`, `access_logs.ONBOARDING_RESET`, `onboarding_logs.ONBOARDING_RESET`
reclassified LIVE → UNREACHABLE, +4; 2026-08-25 cleanup pass: `note_events.ISSUER_RESIDUAL_WITHDRAWAL_CREATED`
removed from source entirely rather than merely reclassified, −1, see §4 item 12), using the precise
classifications above. The full per-store breakdown lives in
[`audit-event-surface-matrix.md`](./audit-event-surface-matrix.md) §7.1, which is now the
authoritative count. Per the preservation contract, **none were removed** — dead enum members, the
unreachable `AML_APPROVED`/`ROLE_ADDED`/`ROLE_REMOVED`/`ONBOARDING_RESET`/`PRODUCT_INACTIVATED`/`PRODUCT_REACTIVATED`
writers, and unused columns are cleanup/wiring candidates for a future, separate product decision,
not something to touch here.

---

## 6. Data-quality / evidence gaps on live writers

| # | Finding | Assessment | Recommended action |
|---|---|---|---|
| 1 | `COD_REJECTED` (corporate onboarding rejection) metadata has no rejection reason field, unlike the individual-onboarding `ONBOARDING_REJECTED` path | PARTIAL | Report — adding a reason requires confirming the COD webhook payload actually carries one; not a zero-query, guaranteed-available field at this call site, so not auto-fixed. **Note:** the *visibility* half of this event (Activity feed exposure) was separately resolved on 2026-08-24; this specific evidence-quality gap (no reason field) remains open/reported. |
| 2 | ~~Automated AML clearance (`org-aml-milestone.ts`) writes `ONBOARDING_STATUS_UPDATED` with `amlApproved: true` in metadata, instead of the dedicated `AML_APPROVED` event used by the admin-driven path~~ **CORRECTED (2026-08-24):** the framing was wrong — there is no live "admin-driven path" to compare against. Re-traced `admin/service.ts:approveAmlScreening` (the `AML_APPROVED` writer) end-to-end: the route, service, SDK method, and `useApproveAmlScreening` hook all exist, but **zero `.tsx` components call the hook**, so `AML_APPROVED` is **UNREACHABLE**, not a parallel live path. `ONBOARDING_STATUS_UPDATED` + `amlApproved:true` (via `maybeAdvanceOrgAfterAmlScreeningCleared`, triggered by both the RegTank webhook and the admin "Refresh" button) is the **only** live AML-progression mechanism today | PARTIAL — no human decision-maker captured for the automated path, because there is currently no live path with a human decision-maker at all | Report — wiring `approveAmlScreening` into the Admin UI (making manual override reachable) or removing it are both product decisions, not something to fix silently; **no manual AML approval UI** is an explicit non-goal of the 2026-08-24 cleanup pass |
| 3 | ~~`APPLICATION_REJECTED` log does not store the rejection reason on the log row itself~~ **RECLASSIFIED (2026-08-25).** BEFORE — flagged as an evidence-quality gap because the audit row has no rejection reason. VERIFICATION — traced the overall/final application-rejection flow end to end, starting from the Admin "Reject" button: the confirm dialog (`AlertDialog`, `page.tsx:1218-1237`) collects no text input at all; the SDK call sends only `{ status: "REJECTED" }` (`api-client.ts:1397-1404`); `updateApplicationStatusSchema` declares only `status` (`admin/schemas.ts:439-443`); `AdminService.updateApplicationStatus` has no reason parameter in its signature (`admin/service.ts:6543-6548`); the issuer notification payload and Activity copy are both static/reason-free (`notification/registry.ts:330-335`; `application-log.ts:260-263,514-517`). **There is no rejection reason available anywhere upstream to have been dropped** — `application_logs.remark = null` on this row is the correct, complete representation of what actually happened. This is a **different flow** from section- and item-level rejection (`SECTION_REVIEWED_REJECTED` / `ITEM_REVIEWED_REJECTED`), which already require a remark client- and server-side (`min(1)` in `admin/schemas.ts:455-457,472-474`) and already store it immutably on `application_logs.remark` (populated via `admin/service.ts:7838-7849`) plus mutably in `application_review_remarks`. AFTER — reclassified from an evidence-quality defect to **PRODUCT_DECISION_REQUIRED / NOT_A_CURRENT_DEFECT**. No runtime fix is needed unless product later decides overall application rejection should require/collect a reason, in which case the existing `application_logs.remark` column and pass-through writer already support snapshotting it with no schema change | ~~PARTIAL~~ **ENOUGH_FOR_CURRENT_FLOW** | No action — not a defect. If product wants a reason on overall rejection in the future, that is a UI/schema/service change (see `audit-event-catalog.md` §2.1 and `audit-event-surface-matrix.md` §2.4), not an audit-writer fix |
| 4 | ~~`APPLICATION_RESUBMITTED` has two writer paths: the rich `amendments/service.ts:resubmitApplication` (full metadata) and a bare `PATCH .../status` with `status=RESUBMITTED` that writes no metadata at all~~ **RESOLVED (2026-08-24)** — confirmed both paths represent the same business action (a resubmission); the bare path now gets a plain, non-invented fallback description ("Application resubmitted for review") instead of rendering blank. Both writer paths and all existing rich metadata are otherwise unchanged; see `audit-event-catalog.md` §2.1 | PARTIAL on the second path | Report — the two paths may represent genuinely different UI actions; conflating them without confirming intent risks changing which flow issuers actually use |
| 5 | `signing_recipients.viewed_at` column exists in schema but is **never written** by any signing code path | MISSING_COMPLIANCE_EVIDENCE | **Investigated 2026-08-24** (documentation only) — full field-by-field SigningCloud evidence inventory now in `audit-event-catalog.md` §4.4. Confirmed `viewed_at`: **NOT_SUPPORTED** (no writer); signer IP and user-agent: **UNKNOWN** (not present in any parsed `SigningCloudProvider` response or webhook payload — do not populate from the envelope creator's request IP, that is a different actor); signed timestamp: **SUPPORTED** (`signing_assignments.signed_at`); signer identity (name/email): **SUPPORTED**; certificate/audit-trail reference: **NOT_SUPPORTED** (no such concept implemented anywhere in `signing/`/`signingcloud/`). Still Report — populating any of the NOT_SUPPORTED/UNKNOWN fields requires a provider-capability investigation with SigningCloud directly, not a code change |
| 6 | A successful gateway deposit **capture** does not write any `gateway_payment_events` row — evidence for a successful deposit lives only in `gateway_payments` status + `investor_balance_transactions` + `note_ledger_entries`, while every *exception* path (name-check, mismatch, expiry, refund) does get an event row | Asymmetric evidence — the "happy path" is the only one without a discrete event | **Investigated 2026-08-24** (documentation only) — traced the full happy-path capture chain (`processInvestorDepositCapture` → `creditCompletedDeposit`) and confirmed it never calls `recordGatewayPaymentEvent`. **Classification: exception/operational log (B)**, not a complete lifecycle log — every writer of a `gateway_payment_events` row represents a deviation from the pure happy path (name-check needed, mismatch, expiry, refund). A future `CAPTURE_SUCCEEDED` event would give one self-contained audit trail instead of requiring a 3-table join, but adding it is a **product/compliance decision**, not something to add silently — **no new payment event** is an explicit non-goal of the 2026-08-24 cleanup pass. Full detail in `audit-event-catalog.md` §3.3 |

---

## 7. Notable things verified as compliant / working correctly

To keep this report honest about what is *not* broken:

- 80% minimum-funding rule and investor refund-on-failure path: **fully implemented and correct**.
- Ledger segregation (`INVESTOR_POOL`/`REPAYMENT_POOL`/`ISSUER_PAYABLE`/`OPERATING_ACCOUNT`) with idempotent posting (`note_ledger_entries.idempotency_key`): **correct**.
- PDPA consent captured as its own record, separate from other legal documents, and enforced before the eKYC step: **correct**.
- Legal document audit trail (version, hash, timestamp, IP, acknowledgement text) for every acceptance: **correct** (Rule C.2 for legal documents specifically — signing-time signer IP remains the separately-documented UNKNOWN gap from the final report).
- Board Resolution / acceptance-document upload → admin per-item review → approve-for-signing gate: **implemented**, via the generic acceptance-documents pipeline (not a dedicated "Board Resolution" table, but functionally equivalent and hard-gated before signing can start).
- Risk-rating capture (A–F) before an invoice offer can be sent, frozen into the prospectus at approval/publish: **correct**.
- Automatic offer/signing expiry enforcement (Rule C.1's "hard blocks + automatic lapse"): **correct** — the expiry job reliably transitions stale offers to `EXPIRED`/`OFFER_EXPIRED` regardless of the reminder-cadence gap noted in §2.

---

## 8. Recommended next steps (for product/compliance sign-off, not auto-implemented)

Ranked by combination of compliance risk and implementation simplicity:

1. **Reminder cadence (§2.1–2)** — config-only change (`deadline-config.ts` reminder arrays) once product confirms the exact required days; lowest engineering effort, highest compliance visibility.
2. ~~**`withdrawal_submitted_to_trustee` notification (§3.1)** — one `sendTyped` call once product confirms issuers should be told; audit trail already proves the underlying event fires.~~ **DONE (2026-08-24)** — see §3.1 for BEFORE/DECISION/AFTER.
3. **Onboarding fee sequencing (§2.3)** — requires product/legal decision on whether to move the fee step after AML approval, since it changes a monetization flow, not just a log.
4. ~~**Admin timeline mislabeling for `CONTRACT_WITHDRAWN` and `AMENDMENTS_SUBMITTED` (§4.5–6)** — pure string changes, but user-facing, so listed for product copy sign-off rather than engineering judgment call.~~ **DONE (2026-08-24)** — both resolved by the copy-consistency pass; see §4 items 5 and 6 for the verified after-state.
5. **Notice of Assignment, guarantor acknowledgment, Risk Statement form, T&C SC-clearance gate (§2.4–6, 2.9)** — larger workflow/entity additions; recommend a dedicated compliance-engineering backlog item per requirement, scoped and estimated separately from this audit review.
6. ~~**Note timeline 50-event cap (§4.9)** — if compliance needs a full, unlimited export path, this is a straightforward addition (a dedicated export endpoint without the `take: 50`) that doesn't change the existing UI behavior at all.~~ **DONE (2026-08-24)** — see §4 item 9 for the verified after-state; UI cap unchanged, export now unlimited.
7. **`PRODUCT_INACTIVATED`/`PRODUCT_REACTIVATED` unreachable writers (§4 item 10)** — either wire a real Admin activate/deactivate action to the existing repository methods, or remove the two dead filter options from `product-logs-panel.tsx`; low engineering effort either way, but a product decision on which outcome is wanted.
8. **Notification-policy items (§3.2)** — `ONBOARDING_APPROVED`, the live AML milestone, `SSM_APPROVED`, and `APPLICATION_SUBMITTED` silence, plus the issuer partial-repayment and gateway refund/name-check gaps, need explicit product sign-off using the classification in §3.2.

No code beyond the one documentation-comment fix (§4.8 / catalog §2.5) was changed in the first
pass; the second same-day pass implemented the additional non-compliance items listed in the
"Second follow-up implementation pass" note in §1 and left a documentation-only investigation trail
for the remaining items above, consistent with the preservation-first contract governing this
codebase's audit system.
